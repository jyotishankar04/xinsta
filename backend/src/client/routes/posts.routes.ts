import { Router } from "express";

const router = Router();

// Post routes
// router.post("/post");
// router.get("/post");
// router.patch("/post/:id");
// router.delete("/post/:id");

export default router;
